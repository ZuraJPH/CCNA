La figura 1 muestra el comando de telnet para un router con la dirección IPv6. La figura 2 muestra el comando para probar la capa de transporte con un comando de telnet con la dirección ipv 6 al puerto ochenta. La figura 3 muestra el comando de telnet para un router con la dirección IPv6. La figura 4 muestra el comando para probar la capa de transporte con un comando de telnet con la dirección ipv 6 al puerto ochenta del router R1.