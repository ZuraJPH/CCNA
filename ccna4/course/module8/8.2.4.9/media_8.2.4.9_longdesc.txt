La figura 1 muestra la topología estándar de laboratorio de CCNA con la asignación IPV4 de direcciones IPv6.  La llamada del router 3 dice que para prevenir los ataques por suplantación, se deben denegar el ingreso de todos los paquetes con dirección de origen 172.16.1.0/24 al puerto serial 0/0/0. La figura 2 muestra la salida del comando show ip access guion lists del router 3, seguida de la salida del comando show ip interface serial cero barra diagonal cero barra diagonal uno barra vertical include access list, seguida de la salida del comando show ip interface gigabit ethernet cero barra diagonal cero barra diagonal uno barra vertical include access list. La figura 3 muestra los comandos para eliminar la lista de acceso cien de la entrada de gigabit ethernet 0/0 y agregarla a la entrada del puerto serial 0/0/1.