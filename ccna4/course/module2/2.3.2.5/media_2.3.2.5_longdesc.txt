La figura 1 muestra el archivo de configuración para dos routers mediante la autenticación PAP entre ellos.  Se resalta la contraseña configurada en cada router, el uso del nombre de host del router que se conecta como los nombres de usuario y el uso de la misma contraseña en todos los routers.  La figura también resalta el comando ppp pap sent guion username R 1 password same one.  En esta línea la contraseña es same one. La figura seis es una actividad interactiva que permite que el estudiante configure la autenticación PAP en una interfaz serial en un router. La figura 3 muestra el archivo de configuración de dos routers que usan autenticación PAP entre ellos.  Se resalta la contraseña configurada en cada router, el uso del nombre de host del router que se conecta como los nombres de usuario y el uso de la misma contraseña en todos los routers.  La figura también resalta el comando PPP authentication CHAP. La figura 4 es una actividad interactiva que permite que el estudiante configure la autenticación CHAP en una interfaz serial en un router.