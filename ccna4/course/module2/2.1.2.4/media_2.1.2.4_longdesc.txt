La figura 1 muestra la salida del comando show interface serial cero barra diagonal cero barra diagonal cero.  En la figura se resalta el estado de la interfaz, que se muestra aquí como activado con el protocolo de línea activado.  La figura también resalta la configuración de encapsulamiento, que es HDLC. La figura 2 muestra una tabla con las seis combinaciones que se pueden obtener con el comando show interfaces serial.  La tabla contiene las causas posibles y sugiere las acciones correctivas para cada una. La figura 3 muestra la salida del comando show controllers serial cero barra diagonal cero barra diagonal cero.  La figura resalta el estado del cable serial. En este ejemplo, es DCE V punto treinta y cinco.