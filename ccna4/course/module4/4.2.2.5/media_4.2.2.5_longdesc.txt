La figura muestra cómo verificar una ACL.  El primer comando se ejecuta en modo exec con privilegios y es: show access guion lists.  Esto muestra las listas de acceso en el router junto con las líneas en cada uno.  Se muestran las dos listas de acceso creadas en la página anterior.  El segundo comando es show ip interface gig cero barra diagonal cero.  Esto muestra las listas de acceso que están configuradas en la interfaz junto con la dirección.  La figura muestra que la lista de salida es browsing y la lista de entrada es surfing.