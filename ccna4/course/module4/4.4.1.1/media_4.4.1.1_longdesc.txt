La figura 1 es un diagrama de flujo que representa el proceso de una ACL de entrada. El primer cuadro de la izquierda dice Interfaz entrante. Esto alimenta un cuadro de decisión que evalúa la primera ACE. Si se configura en "yes", el proceso pasa al cuadro de decisión de permiso o denegación que se describe más adelante. Si se configura en "no", el proceso pasa a la decisión de coincidencia de la segunda ACE. Si se configura en "yes", el proceso pasa al cuadro de decisión de permiso o denegación que se describe más adelante. Si se configura en "no", el proceso pasa a la decisión de coincidencia de la tercera ACE. Si se configura en "yes", el proceso pasa al cuadro de decisión de permiso o denegación que se describe más adelante. Si se configura en "no", se aplica una denegación implícita de todo y el paquete se descarta. A cada paso del proceso, el proceso puede dar "yes" en una coincidencia de ACE. Si la instrucción de ACE contiene una denegación, el paquete se descarta. Si la instrucción de ACE contiene permit, el paquete se envía a la tabla de routing y a la interfaz de destino. La figura 2 muestra un diagrama de flujo que representa el proceso saliente de ACL. El rectángulo de la izquierda muestra la información de la selección de tabla de routing e interfaz saliente y va a la primera decisión de si el primer ACE coincide. Si se configura en "yes", el proceso pasa al cuadro de decisión de permiso o denegación que se describe más adelante. Si se configura en "no", el proceso pasa a la decisión de coincidencia de la segunda ACE. Si se configura en "yes", el proceso pasa al cuadro de decisión de permiso o denegación que se describe más adelante. Si se configura en "no", el proceso pasa a la decisión de coincidencia de la tercera ACE. Si se configura en "yes", el proceso pasa al cuadro de decisión de permiso o denegación que se describe más adelante. Si se configura en "no", se aplica una denegación implícita de todo y el paquete se descarta. A cada paso del proceso, el proceso puede dar "yes" en una coincidencia de ACE. Si la instrucción de ACE contiene una denegación, el paquete se descarta. Si la instrucción de ACE contiene permit, el paquete se envía a la interfaz saliente.