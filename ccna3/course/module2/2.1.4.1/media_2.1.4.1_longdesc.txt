En la figura uno, se muestra el switch uno conectado a los switches dos y tres mediante enlaces troncales.  Los puertos de switch tienen llamadas que indican el modo en el que están configurados.  Los dos puertos para el enlace entre los switches uno y dos se configuran como puertos de enlace troncal.  Los puertos para el enlace entre los switches uno y tres se configuran como puertos en modo dinámico automático. En la figura dos, se muestra la misma LAN. El enlace entre los switches uno y dos formará un enlace troncal, ya que ambos puertos están configurados en modo de enlace troncal.  El enlace entre los switches uno y tres no formará un enlace troncal, ya que ambos están configurados en modo dinámico automático.