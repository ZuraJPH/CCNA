En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP. La red interna aparece resaltada, menos el enlace entre el router dos y el ISP.  En la figura dos, se muestra que el router dos tiene una ruta estática predeterminada que apunta al ISP y que la propaga a los routers uno y tres. En la figura tres, se muestra la sintaxis para configurar una ruta estática predeterminada en el router dos y propagarla mediante el proceso de routing de EIGRP. La figura cuatro muestra el resultado que genera el comando "show i. p. route" en el router dos, que demuestra que la ruta estática está presente. La figura cinco muestra el resultado que genera el comando "show i. p. protocols" en el router dos, que demuestra que el EIGRP redistribuye la ruta estática.