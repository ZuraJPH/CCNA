En la figura uno, se muestra la topología de laboratorio estándar.  El enlace entre el router uno y el router dos se muestra como sesenta y cuatro kbps; el enlace entre el router uno y el router tres se muestra como mil quinientos cuarenta y cuatro kbps; y el enlace entre el router dos y el router tres se muestra como mil veinticuatro cuatro kbps. En la figura dos, se muestra la sintaxis para configurar el ancho de banda máximo que EIGRP usará en una interfaz del router uno y router dos. En la figura tres, se muestra una actividad interactiva que permite que el estudiante practique configurar el ancho de banda máximo que EIGRP usará en una interfaz. En la figura dos, se muestra la sintaxis para configurar el ancho de banda máximo que EIGRP usará en una interfaz del router uno y router dos mediante IPv6.