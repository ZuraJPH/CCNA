En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP y la red interna aparece resaltada, menos el enlace entre el router dos y el ISP. Hay una flecha que sale del router dos y apunta a la red de LAN del router tres. En la figura dos, se muestra la salida que genera el comando “show interface” en el router dos y el router tres.  El ancho de banda para el enlace serial aparece resaltado en color naranja. En la figura tres, se muestra la salida que genera el comando “show interface” en el router dos y el router tres.  El retraso para el enlace serial aparece resaltado en color naranja. En la figura cuatro, se muestra la salida que genera el comando “show ip route” en el router dos.  La métrica para una ruta aparece resaltada en naranja.  A continuación, se muestra el cálculo de la métrica.