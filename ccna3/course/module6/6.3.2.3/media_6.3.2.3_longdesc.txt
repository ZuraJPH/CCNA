En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP y la red interna aparece resaltada, menos el enlace entre el router dos y el ISP. En la figura dos, se muestra la sintaxis para configurar el ancho de banda en las interfaces de los routers uno, dos y tres. En la figura tres, se muestra la salida que genera el comando “show interface” en el router uno y dos para visualizar los parámetros del ancho de banda.  El ancho de banda predeterminado para los enlaces seriales es de 1544 kilobits por segundo, a menos que se especifique.