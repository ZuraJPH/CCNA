En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP. La red interna aparece resaltada, menos el enlace entre el router dos y el ISP. Se utilizan direcciones LAN IPv6. En la figura cinco, se muestra la salida que genera el comando “show ipv6 eigrp neighbors”. La dirección de interfaz link-local, las interfaces locales, el tiempo de espera y el tiempo de actividad aparecen resaltados.