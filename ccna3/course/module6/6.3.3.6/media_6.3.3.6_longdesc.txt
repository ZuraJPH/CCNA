En la figura uno, se muestra la salida que genera el comando “show ip eigrp topology” en el router dos.  El estado, la red de destino, el número de sucesores y la distancia factible aparecen resaltados en color naranja. En la figura dos, se muestra la salida que genera el comando “show ip eigrp topology” en el router dos.  La dirección de siguiente salto, la distancia factible, la distancia notificada y la interfaz saliente de la ruta del sucesor aparecen resaltadas en color naranja. En la figura tres, se muestra la salida que genera el comando “show ip eigrp topology” en el router dos.  La dirección de siguiente salto, la distancia factible, la distancia notificada y la interfaz saliente de la posible ruta del sucesor aparecen resaltadas en color naranja.