En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP y la red interna está resaltada, menos el enlace entre el router dos y el ISP. En la figura dos, se muestra la salida que genera el comando conf t. en el router uno y, a continuación, la salida que genera router signo de pregunta.  La salida indica los diferentes protocolos de routing disponibles. En la figura tres, se muestra el comando “router eigrp one” en cada uno de los routers.