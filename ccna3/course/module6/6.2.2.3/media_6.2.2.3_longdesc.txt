En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP y la red interna aparece resaltada, menos el enlace entre el router dos y el ISP. En la figura dos, se muestra la salida que genera el comando “show ip route” en el router uno.  Las rutas EIGRP aparecen resaltadas en naranja. En la figura tres, se muestra la salida que genera el comando “show ip route” en el router dos.  Las rutas EIGRP aparecen resaltadas en naranja. En la figura cuatro, se muestra la salida que genera el comando “show ip route” en el router tres.  Las rutas EIGRP aparecen resaltadas en naranja.