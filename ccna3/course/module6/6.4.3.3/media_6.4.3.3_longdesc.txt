La figura uno muestra la salida del comando “show ipv6 route” en el router uno.  Las rutas EIGRP aparecen resaltadas en naranja para indicar que utilizan direcciones IPv6. La figura dos muestra la salida del comando “show ipv6 route” en el router dos.  Las rutas EIGRP aparecen resaltadas en naranja para indicar que utilizan direcciones IPv6. La figura tres muestra la salida del comando “show ipv6 route” en el router tres.  Las rutas EIGRP aparecen resaltadas en naranja para indicar que utilizan direcciones IPv6.