En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP y la red interna aparece resaltada, menos el enlace entre el router dos y el ISP. En la figura dos, se muestra la sintaxis para configurar las instrucciones de la red mediante eigrp en el router uno. En la figura tres, se muestra la sintaxis para configurar las instrucciones de la red en eigrp para el router dos.  Inmediatamente después de esto, hay una notificación doble que indica una adyacencia de vecinos.