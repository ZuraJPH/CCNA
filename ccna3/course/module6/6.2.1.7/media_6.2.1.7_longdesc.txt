En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP y la red interna aparece resaltada, menos el enlace entre el router dos e ISA. La interfaz serial del router dos que apunta al router tres está encerrada en un círculo, incluida la dirección IP del punto nueve. En la figura dos, se muestra la sintaxis para configurar las instrucciones de la red de EIGRP para la interfaz serial que aparece en el círculo en el router dos utilizando la máscara de comodín (wildcard). En la figura tres, se muestra la salida que genera el comando "show running dash config" en el router dos.  En la sección de EIGRP, la red que se acabo de configurar aparece resaltada. La figura cuatro corresponde a una actividad interactiva que permite que el estudiante realice la configuración de EIGRP en el router tres.