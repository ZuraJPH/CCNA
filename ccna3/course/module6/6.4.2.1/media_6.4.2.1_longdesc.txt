En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP. La red interna aparece resaltada, menos el enlace entre el router dos y el ISP. Se utilizan direcciones LAN IPv6.  En la figura dos, se muestra la salida que genera el comando “show running dash config” en el router uno, y se incluye la configuración de la interfaz. En la figura tres, se muestra la salida que genera el comando “show running dash config” en el router dos, y se incluye la configuración de la interfaz. En la figura cuatro, se muestra la salida que genera el comando “show running dash config” en el router tres, y se incluye la configuración de la interfaz.