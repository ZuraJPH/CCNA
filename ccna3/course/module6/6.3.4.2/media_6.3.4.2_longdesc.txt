En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP y la red interna aparece resaltada, menos el enlace entre el router dos y el ISP. Hay una flecha que sale del router dos y apunta a la red de LAN del router tres, y el router uno aparece dentro de un círculo con la frase “feasible successor” (sucesor factible) a su lado.  El enlace serial entre el router dos y el router tres está desactivado. En la figura dos, se muestra la salida que genera el comando “show ip eigrp topology” en el router dos con las rutas del sucesor y sucesor factible resaltadas en color naranja. En la figura tres, se muestra la salida que genera el comando “debug eigrp fsm” en el router dos.  Luego, la interfaz serial se apaga y se muestran mensajes sobre el proceso DUAL. En la figura cuatro, se muestra la salida que genera el comando “show ip route” en el router dos con el nuevo sucesor. En la figura cinco, se muestra la salida que genera el comando “show ip eigrp topology” en el router dos con la nueva ruta del sucesor.