En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP; la ruta interna aparece resaltada, menos el enlace entre el router dos y el ISP.  Se utilizan direcciones de la red LAN de IPv6. En la figura dos, se muestra la sintaxis para configurar una dirección link-local IPv6 en las interfaces del router uno. En la figura tres, se muestra la sintaxis para configurar una dirección link-local IPv6 en las interfaces del router dos. La figura cuatro corresponde a una actividad interactiva que permite que el estudiante configure las interfaces en el router tres con direcciones link-local. En la figura cinco, se muestra la salida que genera el comando “show ipv6 interface brief” en el router uno con las direcciones link-local resaltadas para cada interfaz.