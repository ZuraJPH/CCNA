En la figura uno, se muestra la sintaxis para configurar el routing IPv6 para EIGRP en el router dos. En la figura dos, se muestra la sintaxis para configurar la ID de router EIGRP en el router uno. En la figura tres, se muestra el comando “no shut down” después de configurar la ID de router EIGRP en el router uno.  Esto activará el proceso de routing EIGRP, que está desactivado de manera predeterminada. En la figura cuatro, se muestra el proceso de configuración de EIGRP para IPv6 incluida la ID de router y el comando no shut down en el router dos. La figura cinco corresponde a una actividad interactiva que permite que el estudiante configure EIGRP para IPv6, incluida la ID de router y el comando no shut down en el router tres.