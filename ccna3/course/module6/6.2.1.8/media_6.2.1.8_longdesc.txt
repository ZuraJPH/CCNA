En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP y la red interna aparece resaltada, menos el enlace entre el router dos y el ISP. Cada router está enviando un paquete de saludo fuera de la interfaz de la red LAN. En la figura dos, se muestra la sintaxis para configurar una interfaz pasiva en el router uno y el router tres. Esto evita que se envíen actualizaciones de routing por las interfaces de la red LAN. En la figura tres, se muestra la salida que genera el comando “show ip protocols” en el router tres. Las interfaces pasivas aparecen resaltadas. La figura cuatro corresponde a una actividad interactiva que permite que el estudiante realice la configuración de EIGRP con el comando de interfaz pasiva.