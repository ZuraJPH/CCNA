En la figura uno, se muestra la sintaxis para activar IPv6 EIGRP en cada interfaz en el router uno y el router dos. La figura dos corresponde a una actividad interactiva que permite que el estudiante active IPv6 EIGRP en cada interfaz en el router tres. En la figura tres, se muestra la sintaxis para configurar una interfaz pasiva para EIGRP utilizando IPv6. El comando se introduce en el proceso de routing y es el mismo que el comando que se utiliza para IPv4.