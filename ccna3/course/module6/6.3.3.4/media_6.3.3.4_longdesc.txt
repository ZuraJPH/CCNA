En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP y la red interna aparece resaltada, menos el enlace entre el router dos y el ISP. Hay una flecha que sale del router dos y apunta a la red de LAN del router tres, y el router uno aparece dentro de un círculo con la frase “feasible successor” (sucesor factible) a su lado. En la figura dos, se muestran los tres routers conectados con enlaces seriales.  El router uno envía al router dos y al router tres su distancia factible a la red LAN del router tres.  La salida que genera el comando “show ip route” en el router uno se presenta a continuación con la métrica o la distancia factible resaltadas en color naranja. En la figura tres, se muestra la salida que genera el comando “show ip route” en los routers uno y dos.  La distancia factible aparece resaltada.  En la leyenda, se muestra la pregunta “Does it meet the feasibility condition?” (¿Cumple con la condición de factibilidad?). En la figura cuatro, se muestran los tres routers conectados con enlaces seriales.  El router dos envía al router uno la distancia factible a la red LAN del router tres. El router uno es el nuevo sucesor.