En la figura uno, se muestra el resultado que genera el comando show i.p. protocols en un router. En la figura dos, se muestra el resultado que genera el comando show i.p. route barra vertical begin gateway. En la figura tres, se muestra el resultado que genera el comando show i.p. o.s.p.f. neighbor en un router. En la figura cuatro, se muestra el resultado que genera el comando show interfaces gigabit ethernet cero barra oblicua cero en un router. En la figura cinco, se muestra el resultado que genera el comando show i.p. interface gigabit ethernet cero barra oblicua cero en un router. En la figura seis, se muestra el resultado que genera el comando show i.p. interface brief en un router. En la figura siete, se muestra el resultado que genera el comando show protocols en un router. En la figura ocho, se muestra el resultado que genera el comando show c.d.p. neighbors en un router. En la figura nueve, se muestra una actividad interactiva que permite que el estudiante practique usar los comandos que se vieron en esta sección.