En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP. Se resalta la red interna, menos el enlace entre el router dos y el ISP. En la figura dos, se muestra la salida del comando "show ip route" en el router uno. En la figura tres, se muestra la salida del comando "show ip protocols" en el router tres.  Se resaltan las redes para las que el router tres realiza routing. En la figura cuatro, se muestra la salida del comando "show running barra diagonal config barra vertical section router ospf" en el router tres. En la figura cinco, se muestra la sintaxis para configurar una red para OSPF. En la figura seis, se muestra la salida del comando "show ip route ospf" en el router uno.