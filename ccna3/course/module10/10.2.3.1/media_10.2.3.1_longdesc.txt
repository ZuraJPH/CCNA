En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP. Se resalta la red interna, menos el enlace entre el router dos y el ISP. En la figura dos, se muestra la salida del comando "show ipv6 protocols" en el router uno. En la figura tres, se muestra el resultado del comando "show ipv6 neighbors" en el router uno. En la figura cuatro, se muestra la salida del comando "show ipv6 ospf interface s. cero barra diagonal cero barra diagonal cero" en el router uno. En la figura cinco, se muestra la salida del comando "show ipv6 ospf" en el router uno. En la figura seis, se muestra la salida del comando "show ipv6 route ospf:" en el router uno.