En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP. Se resalta la red interna, menos el enlace entre el router dos y el ISP. En la figura dos, se muestra la salida del comando "show ipv6 route ospf" en el router uno. En la figura tres, se muestra la sintaxis para configurar una ruta estática predeterminada con IPv6 al proveedor de servicios.  Debajo, se muestra la sintaxis para propagar esa ruta estática predeterminada a través del proceso de routing OSPF.