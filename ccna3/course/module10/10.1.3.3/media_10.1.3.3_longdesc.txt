En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP. Se resalta la red interna, excepto el enlace entre el router dos y el ISP. En la figura dos, se muestra la sintaxis para configurar los temporizadores de intervalos de saludo y de tiempo muerto en la interfaz serial cero barra diagonal cero barra diagonal cero en el router uno para IPv6. En la figura tres, se muestra la salida del comando "show ipv6  ospf neighbor". En la figura cuatro, se muestra la sintaxis para configurar el temporizador de saludo y en la interfaz serial cero barra diagonal cero barra diagonal cero en el router dos. En la figura cinco, se muestra la salida del comando "show ipv6  ospf interface s. cero barra diagonal cero barra diagonal cero barra vertical include timer" y el comando "show ipv6  ospf neighbor" en el router dos.