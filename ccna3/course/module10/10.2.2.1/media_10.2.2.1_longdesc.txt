En la figura uno, se muestra el diagrama de laboratorio estándar con el enlace extra entre el router uno y el router tres.  El router dos está conectado al ISP. Se resalta la red interna, menos el enlace entre el router dos y el ISP. En la figura dos, se muestra la salida del comando "show ip route" en el router uno. En la figura tres, se muestra la salida del comando "show ip interface brief" en el router uno y un ping exitoso. En la figura cuatro, se muestra la salida del comando "show ip ospf interface serial cero barra diagonal cero barra diagonal cero". En la figura cinco, se muestra la salida del comando "show ip protocols" en el router uno. Se resalta la interfaz Serial cero barra diagonal cero barra diagonal cero para identificarla como una interfaz pasiva. En la figura seis, se muestra la sintaxis para configurar la interfaz Serial cero barra diagonal cero barra diagonal cero de modo que no sea pasiva. En la figura siete, se muestra la salida del comando "show ip route ospf" en el router uno.