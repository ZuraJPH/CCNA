La figura uno muestra una topología de OSPFv3 multiárea IPv6. La figura dos muestra la salida recibida al utilizar el comando show ipv6 protocols.  La imagen resalta la cantidad de áreas a las que pertenece el router y las redes que se están enrutando. La figura tres muestra la salida recibida del comando show ipv6 ospf interface brief y resalta la columna que muestra las áreas en las que cada interfaz está conectada. La figura cuatro muestra la salida del comando show ipv6 route ospf, que verificará las rutas OSPF multiárea.