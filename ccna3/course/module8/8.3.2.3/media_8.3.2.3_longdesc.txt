En la figura uno, se muestran los comandos necesarios para configurar una dirección link-local específica en una interfaz.  El primer comando es ipv6 address f e 8 0 dos puntos dos puntos 1 link guion local. Este comando configura la dirección para la interfaz de Gigabit Ethernet.  La figura aplica la misma dirección a ambas interfaces seriales con el mismo comando.  En la figura dos, se muestra la salida del comando show ipv6 interface brief y se resalta la dirección link-local en cada interfaz. La figura tres es un elemento de medios interactivos que permite que el estudiante configure direcciones link-local en el router R2.