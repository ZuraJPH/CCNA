En la figura uno, se muestra la topología de referencia de OSPF y se han agregado valores de ID de router.  El router R1 tiene una ID de router configurada de uno punto uno punto uno punto uno.  El router R2 tiene una ID de router configurada de dos punto dos punto dos punto dos.  El router R3 tiene una ID de router configurada de tres punto tres punto tres punto tres.  En la figura dos, se muestran los comandos utilizados para asignar una ID de router.  La figura comienza en el modo de configuración global.  El primer comando es router ospf. 10.  Con este comando, se configura OSPF en el router y se establece el proceso en 10.  El siguiente comando es router guion id uno punto uno punto uno punto uno.  Con este comando, se establece la ID del router.  La figura muestra la salida del comando show ip protocols. En la salida, se resalta la línea que muestra la ID del router. La figura tres es un elemento de medios interactivos que permite que el estudiante asigne una ID de router a un router.